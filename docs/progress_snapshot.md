# 进度快照与研讨台账（计划层：以车厢为中心 + POI + 调度计划）

> **用途**：上下文吃紧 / 换新会话时的**续期入口**。读这一篇就能接上进度，不必回翻对话。
> **快照时间**：2026-09-15（文档快照；对应提交 `ef78232`）。详细规格与证据在各专项文档，本文只做**索引 + 状态 + 待办**。
> **维护约定（重要）**：今后每达成一个决策 / 落地一段代码 / 新增一个待研讨问题，
> **必须回到本文对应小节更新**——不要把新结论只留在对话里（本文件的存在就是为了防这件事）。

---

## 0. 项目目标与收口条件（当前口径）

- **demo 收口 = 计划式自动驾驶落地 + 项目开源**（`docs/roadmap.md` #7 / #8）。
- **demo 不做**：真实物理扩展（#3）、LOD（#4）、瓦片化（其实已实现）、**货物/装载**（#10）。
- **有意取舍不修**：无物理倒车（负向推进原语）、无碰撞模型（A 碾过 B 是视觉穿过）。
- 编辑器（Steps 0–6 + 吸附 + 空间索引）与信号系统（Step 1–6）**已完成并人工验收**；
  #1 连挂/解挂（含折返/寻路联合场景）与 #2 会话持久化**已人工验收通过**。

## 1. 当前状态（一句话）

**规划已封口，正在按 `docs/plan_layer_roadmap.md` 的 P0~P9 实现**：
**P0（分段器）/ P1（计划数据类型）/ P2（锚点解析器）/ P3a（冻结路径）/ P4（拓扑变更防火墙）已完成**
（`103f7a0` / `44c240c` / `ef78232` / `7441796` / 当前工作树），
**下一步 = P3b（计划机制接入列车，首个"有行为"阶段）→ P5（最小交互）**。

### 1.1 研究对象（一句话）

让列车"**按计划自动运行**"：**计划归控制车（车厢）所有**（"每个控制车都有一个司机，
有自己的任务，与列车无关"），编组每帧**遴选**并**投影**成 `route`/`goal`；
目标点用**图上已有的节点**表达，条目在确认时冻结为**完整有向边路径**；锚点与控制点
是编辑期的路径限定元数据，不在运行期触发寻路。
※ 原文为"目标点用 POI 表达"——**Q22-1 已改为"先用现成图要素"**，POI 移出关键路径。

✅ **2026-09-10 追加（重大架构，已封口）**：计划从"**目标**（车站）"升级为
"**目标 + 路径限定**"（锚点级 + 部分控制点）。**Q21/Q22/Q23 共 17 项全部拍板**，
**实施路线 = `docs/plan_layer_roadmap.md`（P0~P9，无一项被卡住）**，
提案与代码核对见 `docs/path_level_plan.md`。
⇒ **规划阶段已封口，下一步从 P0（分段器与校验）开始实现。**

## 2. 已拍板清单（Q1~Q23；截至 2026-09-10 的研讨轮次）

| 编号 | 结论（一句话） | 权威位置 |
|---|---|---|
| **Q1** | 持久化边界 = **方案 A**：**域数据（物理/载货/计划）归车厢**；**运行期派生**（v / occupancy / controller / authority / 选中计划投影出的 route·goal）归编组、可随时重算；**运动学与位置还原不重写** | `wagon_centric_data.md` §5 Q1 |
| **Q2** | 控制/动力**四种组合都支持**；**控制是决定性属性**（`have_control` 与身份同级），**动力只是物理参数**（`P_rated` 与 mass 同级） | §5 Q2 |
| **Q3** | **"只读"是设计准则、不做强制**；"自力更新" = **只有 wagon 类下属逻辑能改 wagon 字段** ⇒ **装载反过来：车厢凭信号从站台"抢"货** | §5 Q3 |
| **Q4** | **克隆 = 新车**（缺省值、不继承状态） | §5 Q4 |
| **Q5** | `priority` 相同 → **`wagon_id` 取小者胜**（遴选键 `(-priority, wagon_id)`，结果稳定） | §5 Q5 |
| **Q6** | **无控制车的段静止**；**计划只有控制车才能持有**；**没有"挂起"标记**（挂起 = 指令指针停在"等待…"命令上，仍在跑循环） | §5 Q6 + Q13 |
| **Q7** | **单向管道**：调度计划 → **冻结路径** → 预约路径 → 物理模拟 → 位移；编辑期才允许"计划 → 寻路需求 → 候选路径 → 冻结"，**遴选每帧进行** | §5 Q7 |
| **Q8** | **POI 最小单元 = "有向点"**（1 node + 1 edge，edge 只给方向不给长度） | `poi.md` §1 |
| **Q9** | ✅ 已执行：`data_log` 迁入车厢（容器 `WagonDataLog`，拆分/归并记账删除） | §5 Q9 |
| **Q10** | ✅ 已执行：删死代码（`Consist.velocity/acceleration`、`reversed_consist/reversed_config`） | §5 Q10 |
| **Q11** | 计划的**悬空引用 → 该条命令跳过执行、计划仍保留**（OpenTTD「非法的调度计划」） | §5 Q11 |
| **Q13** | 事件 = **直接调用车厢的对应方法**（不做事件总线）；**"挂起"不存在**，⇒ 计划类型**不要**加 `suspended`/`paused` | §5 Q13 |
| **Q14** | **指令指针在控制车**；**步进 = 列车到达目标后直接调用控制车方法**；必须满足**单一步进者** + **到达事件幂等**（可复用 `GameLoop.run()` 的 `moving_just_stopped` 事件帧） | §5 Q14 |
| **Q15** | **计划的根本特征**：条目 + 指针；**走完跳回第一项**（循环）；最基本命令 **`goto` → 建筑/POI**；**demo 重点做 goto**（连挂/解挂视情况加入）；**空计划 = 列车停车等待** | §5 Q15 |
| **Q21** | ✅ **重大架构已定（2026-09-10）**：**路径级调度计划**——**Q21-1 = 锚点级 + 部分控制点**；**Q21-2 = 与目标级条目并存，锚点比目标级"低一层"**（只有目标条目携带逻辑，锚点序列**解析后只是路径** ⇒ 不是第二种条目类型，而是条目的**路径限定属性**）；**Q21-3 = 拒绝编辑/删除被引用的要素**（现在不适配图变化，就先保护） | `path_level_plan.md` §3/§4.1 |
| **POI** | 三类基本 POI：无向点（Node，=2 连接，Waypoint）/ **有向点**（Node+Edge，≤2）/ **路段**（Simple_Segment，站台或站线）；**一切建筑 = POI 的集合**（火车站 = 4 个路段）；**O9**：站台**带范围/长度**。⚠ **2026-09-10（Q22-1）：POI 已移出关键路径**——锚点先用现成图要素，将来叠加为"带名字的锚点" | `poi.md` §1–§4、§7 O9 |
| **Q16** | ✅ **已拍板方向**：解挂命令**声明"后段计划来源"**（沿用原计划/等待连挂/接管某计划…，具体枚举值待任务 3）；**实现口径**：**为后段构造/指派它自己的计划（挂在其控制车上）**——**不共享同一对象**（px-patch 共享 OrderList 那条不能照抄） | §5 Q16 |
| **Q17** | ✅ **已封口**（Q23-8/1/3）：①跳转目标 = **demo 不做显式跳转**（既不用下标也不用标签，循环靠"走完回绕第一项"）②**步进语义表已定稿**（`plan_layer_roadmap.md` §2.3）③失效条目 = **可见 + 标红 + 原因 + 不自动删** | §5 Q17 |
| **Q18** | ✅ **已封口（Q23-2）= 分永久/暂时**：引用对象**不存在** → 跳过（Q11）；对象**在**但条件不满足 → **原地等待**。判据是"对象还在不在" | `plan_layer_roadmap.md` §4.4 |
| **Q19** | ✅ **已封口（Q23-7）**：**`goto_couple` 进 demo 但只做最简形态**（目标 = 指定列车/端头车钩，**不做全图搜索与限制**；解挂保持 K 键手动） | `plan_layer_roadmap.md` §1/§2.2 |
| **Q22** | ✅ **五项全定**：锚点用现成图要素（POI 移出关键路径）/ 引用落在物理要素 / 锚点是**硬约束** / **最小交互**创建路径 / 控制点 = **道岔处选出口** | `path_level_plan.md` §4.2 |
| **Q23** | ✅ **九项全定**（数据模型 / 失效分永久暂时 / 失效可见+原因 / 删除拒绝·切分改写引用 / 仅停放可编辑 / **demo 不落盘** / 连挂最简形态进 demo / **不做显式跳转** / tick 不接线） | `plan_layer_roadmap.md` §1 |
| **实施路线** | ✅ **`docs/plan_layer_roadmap.md` P0~P9**（P0 → P1 → P2 → **P3a 冻结路径 → P4 拓扑防火墙 → P3b 接入列车** → P5 交互 → P7 连挂条目 → P9 收口+开源；**P6 落盘 / P8 tick 取消**） | —— |
| **连挂=不交割计划** | ✅ **已定**（三轮调研带出并已确认）：px-patch 是"**合并后用『前往方』的计划、可配置切换**"（**交割一份计划**）；我们**相反**——合并后由**遴选**出的控制车执行**它自己的**计划，**落选方的计划留在其车厢上不动** | `research_pxpatch.md` §6.2 |
| **R10** | ✅ **已定**：连挂侧照搬 px-patch（**交割指针后若停在等待命令上就强制步进**）；解挂侧由 Q16 天然消解；剩余 = 写进任务 3 的步进语义表 | §9 R10 |
| **结构** | **`WagonConfig`（只读配置）+ `Wagon`（运行时对象，带 `tick(dt)`，空实现未接线）**；用"配置与状态分家"代替语言层强制只读 | §9.1 + 下方 §3 |
| **参考** | ✅ **外部调研已完成**（2026-09-10）：结论见 **`docs/research_pxpatch.md`**（3 条可借鉴 + 3 条陷阱 + 4 条待确认） | —— |

## 3. 代码状态（截至快照）

**已落地的提交**（均在 2026-09-10；每次都有全套回归 + 存档往返验证）：

| 提交 | 内容 |
|---|---|
| `4919434` | **T0**：归属规则 + 四桶写进 `model/wagon.py`、`model/train_entity.py` 模块 docstring 与 `docs/session_persistence.md` §2.1 |
| `fc6808f` | **T1**：删死代码（`Consist.velocity/acceleration`、`reversed_config`/`reversed_consist`） |
| `0c62497` | **T2**：`have_control`/`priority`（惰性）、`data_log` 迁入车厢、`Consist.control_cars()`、physics 去身份（`DEFAULT_PHYSICS` 共享）、`couple_with(self)` 自反断言 |
| `36894e2` | **拆分**：`WagonConfig`（只读配置）+ `Wagon`（运行时对象 + `tick`）；`Consist.wagons: list[Wagon]`，裸配置自动包装；只读属性委托给 `config`；新增 `create_simple_car()` |
| **P0** | **计划层 P0：分段器与校验** —— 新增 `model/segments.py`（`Segment`/`SegmentPartition`/`is_cut_node`/`build_partition`/`validate_partition`）与 `tests/test_segments.py`；**纯模型、无消费点、不改既有行为**（`simple_segment_from_endpoint` 语义未动，测试内有对照断言）。**回归 15/15 通过**（新增第 15 个脚本）；真实存档只读未改：**22 节点 / 24 边 → 6 段**、切割点 = 4 个道岔、段长 400.00/405.29/1071.24 m、**段内双向逐点非法转向 0 个**。详见 `plan_layer_roadmap.md` §3.1 |
| **登记** | ⚠ **`simple_segment_from_endpoint` 的急折盲区**（P0 施工时发现）：只看度数、不看 `turn_allowed` ⇒ 走不通的链会被当成一整段、长度被高估（影响"折返可行性"与信号"安全停车位置"两处既有判断）。当前存档此类节点 = **0** ⇒ 不咬人。**用户拍板"不修"**，就地登记于 `model/rail_network.py` 该函数 docstring + `plan_layer_roadmap.md` §6.4。**通用准则（用户口径）**：**分段宁可更激进——多切总是安全的，少切才会把走不通的链当成一段** |

| **P1** | **计划层 P1：计划数据类型** —— 新增 `model/plan.py` + `tests/test_plan.py`；**纯数据类型、无消费点**（不被任何运行链路引用）。`PlanCommand`（goto / goto_couple / wait_couple，**无跳转**）+ `Goal=(edge_id,t,direction)` + `Anchor(node_id, exit_edge_id)`（**出口给定时即控制点**）+ `TrainRef(wagon_id, end)`（连挂目标按**车厢 id** 指认，比列车对象引用稳）+ `PlanItem` + `Plan(items, pointer)`；`validate(network=None)` 同时落地 **Q23-2 的"永久失效"判据**（节点/边不存在、出口不接在该节点上、死出口）；指针语义只做纯位移（**走完回绕第一项**），**步进策略属 P3**。**回归 16/16 通过**。详见 `plan_layer_roadmap.md` §3.1 |

| **P2** | **计划层 P2：锚点解析器** —— 新增 `model/plan_path.py` + `tests/test_plan_path.py`；**纯模型、无消费点**。它现在是**编辑期构造器**：无锚点时委托 `find_path_from_point`，有锚点时逐锚点 DP；P3a 会将结果冻结为固定路径，P3b 运行期不得调用它。其余验证同上：17/17 回归通过，真实存档对比与既有寻路等价，锚点可强制改走另一侧。 |

| **P3a** | **固定路径模型** —— `FixedRoute(edges, start_offset, end_offset)` 加入 `PlanItem.fixed_route`，`ResolvedPath.freeze()` 完成编辑期构造→确认期冻结。严格校验入口拒绝未冻结条目；校验完整路线连续性、转向、终点和距离。`conflicts_with_signal()` 为 P4 的单向 PBS 放置准入提供纯模型判据。新增 `tests/test_fixed_route.py`；**全套 18/18 回归通过**。仍无运行消费点。 |

| **P4** | **拓扑变更防火墙** —— 新增 `model/topology_guard.py`，以方向保序的 `EdgeSplit` 原子改写运行 `route`、目标、冻结路线与控制点出口；`GameLoop` 统一保护切边、删除/节点合并与单向 PBS 放置。车身占用或信号所在边不可切；双端 BUILD 先整体预检，避免半完成拓扑。新增 `tests/test_topology_guard.py`、`tests/test_topology_guard_game_loop.py`、`tests/test_topology_guard_editor.py`；**全套 21/21 回归通过**。仍无计划执行消费点。 |

**P4 已完成，下一步 = P3b**：所有切边/删除都经拓扑防火墙原子改写或拒绝；放置信号前
拒绝会从背面封死固定/运行路线的槛位。接下来将控制车计划接入列车，从固定路径投影
`route`/`goal`，运行期零 Dijkstra，并保留信号等待、到达步进、R10 与 R11 断言。
P3b 是首个"有行为"阶段，之后每步跑全套回归；触及连挂/解挂/折返带须请你人工复测。
✅ **临时调试入口已拍板**：`Shift + 右键` 追加一条**冻结**的 `goto`；连续追加以上一条
终点为构造起点，P5 正式编辑器完成后移除。

**读代码时要知道的三件事**：
1. `Consist.wagons` 里是 **`Wagon`**；它对 `length`/`mass`/`bogies`/`coupler_*`/`P_rated`/
   `have_control`/`priority`/`is_powered`/`bogie_spacing`/`wagon_id` 都是**委托**到 `config`。
2. 现有链路**已经符合 Q7 管道**（`_issue_goal_order` → `assign_route` →
   `TrainDispatcher`/`reserve_path` → `update`/physics → `s`）；**只缺最上游"调度计划"那一段**。
3. **POI 不需要"挖成本图"**：`find_path` 的 `goal_directed` 判据是 `cur == goal_directed`
   （必须精确走到那条有向边），"从固定方向到达"已由搜索保证；有向点 → `DirectedEdge`
   用现成的 `_directed_from(network, edge_id, node_id)`。仍需补：**POI 带 `t`**、
   **路段的多目标寻路**（现无入口）。

**⛔ 已在代码里但"不要接消费点"**（接了就等于提前把行为定死）：
`have_control`、`priority`、`Consist.control_cars()`、`Wagon.tick()`、`create_simple_car()`。

## 4. 规划封口状态（**已封口，可进入实现**）

> **结论**：Q21+Q22+Q23 共 **17 项全部有答案**；实施阶段表（`plan_layer_roadmap.md` §3）
> 的"被谁卡住"一列**已清空**；**剩余只有"明确延后"与"实现期微决策"两类**。
> 权威清单：**`docs/plan_layer_roadmap.md` §6.2（延后项）/ §6.3（微决策）**。

### 4.0 封口速查（Q21/Q22/Q23）

| 组 | 结论 |
|---|---|
| **Q21** | **1** = 锚点级 + 部分控制点；**2** = 锚点比目标级低一层（条目的"路径限定"，不是第二种条目）；**3** = 拒绝编辑/删除被引用的要素 |
| **Q22** | **1** = 锚点用现成图要素（POI 移出关键路径）；**2** = 引用落在物理要素（段只作派生视图）；**3** = 锚点是**硬约束**；**4** = 最小交互创建路径（不做面板 GUI）；**5** = 控制点 = **道岔处选出口** |
| **Q23** | **1** = 数据模型定稿（`plan_layer_roadmap.md` §2）；**2** = 失效分**永久/暂时**；**3** = 失效条目**可见+标红+原因+不自动删**；**4** = **删除拒绝、切分改写引用**；**5** = **仅停放可编辑、确认即生效**；**6** = ⚠**demo 不落盘**；**7** = 连挂条目**最简形态进 demo**；**8** = ⚠**不做显式跳转**；**9** = `Wagon.tick` **不接线** |
| **命令集合** | `goto` / `goto_couple` / `wait_couple`（**无跳转**）；循环 = 走完回绕第一项；**一次到达事件最多走一圈**，整圈无可执行 ⇒ 停车等待并提示 |
| **实施路线** | `plan_layer_roadmap.md` **P0 → P1 → P2 → P3a 冻结路径 → P4 拓扑防火墙 → P3b 接入列车 → P5 交互 → P7 连挂条目 → P9 收口+开源**；P6/P8 取消 |

### 4.1 明细（保留证据链）

0. ✅ **【重大架构·规划已封口】路径级调度计划——Q21/Q22/Q23 共 17 项全部有答案**
   （2026-09-10）。**实施路线以 `docs/plan_layer_roadmap.md` 为准（P0~P9，已封口，
   无一项被卡住）**；提案与代码核对含实测见 `docs/path_level_plan.md`。
   **已定**：**Q21-1 = 锚点级 + 部分控制点**；**Q21-2 = 锚点比目标级"低一层"
   （只有目标条目携带逻辑，锚点序列解析后只是"路径"⇒ 不是第二种条目，而是条目的
   "路径限定"属性）**；**Q21-3 = 拒绝编辑/删除被引用的要素**；
   **Q22-1 = 锚点用现成图要素**（⇒ **POI 的 O1~O9 移出关键路径**）；
   **Q22-2 = 引用落在物理要素**（段只作派生视图）；**Q22-3 = 锚点是硬约束**
   （解析不出按失效语义处理、**绝不自动绕行**）；**Q22-4 = 最小交互创建路径**
   （点几下，不做面板 GUI）；**Q22-5 = 控制点 = 道岔处选出口**（= 现实进路）；
   **Q23-1 = 数据模型按 `plan_layer_roadmap.md` §2 定稿**；
   **Q23-2 = 失效分永久/暂时**（对象不在 ⇒ 跳过；对象在但条件不满足 ⇒ 原地等待）；
   **Q23-3 = 失效条目"可见 + 标红 + 原因 + 不自动删"**；
   **Q23-4 = 删除拒绝；切分（放信号/建造截断）放行并自动改写引用**；
   **Q23-5 = 仅停放列车可编辑计划、确认即生效**；
   ⚠ **Q23-6 = demo 不落盘**（⇒ P6 取消，几何锚定延后）；
   **Q23-7 = 连挂条目进 demo 的最简形态**（`goto_couple` + `wait_couple`，**不做
   全图搜索与限制**；解挂保持 K 键手动）；
   ⚠ **Q23-8 = demo 不做显式跳转**（⇒ 既不用下标也不用标签，"走完回绕第一项"即循环；
   用户示例里那条"3 空命令"**自然消解**）；
   **Q23-9 = `Wagon.tick` 不接线**（demo）。
   **下一步 = 从 P0（分段器与校验）开始实现。**
   ⭐ `path_level_plan.md` 的**关键核对结论**：① **今天已是"算一次照着走"**
   （全仓唯一跑 Dijkstra 的是 `GameLoop._find_route`，调用者只有右键与 K 连挂），
   运行期路径本来就是数据（`occupancy.route: list[DirectedEdge]`）⇒ 担心的
   "切断消费顺序/架空寻路"**在机制上不成立**、**执行层零改动**；
   ② **"不重不漏的分段"已用真实存档实测成立**（22 节点/24 边 → **6 段覆盖全部边**，
   4 个道岔各 4 个合法转向）⇒ 全图选路自由度只有 4 个点，**贵的不是信息量而是人机接口**；
   ③ ⚠ **"段内可通行"不自动成立**（`simple_segment_from_endpoint` 不看
   `turn_allowed`，已构造 90° 急折反例）⇒ P0 的切分规则要加"过境转向不许可处也切"。

1. **POI 建模**（roadmap #9 第 2 项）：**⏸ 已移出关键路径**（Q22-1）——
   `docs/poi.md` §7 的 **O1~O9**（有向点代表什么 / 建筑→POI 解析分配 / 属性面 /
   放置交互 / id 与落盘 / 与信号·block 的关系 / 路段多目标 / 远期两类 / **O9 站台带长度**）
   **不阻塞实现**，将来叠加为"带名字的锚点"。
2. **计划/命令类型**（roadmap #9 第 3 项）：✅ **已定稿**——命令集合 =
   `goto` / `goto_couple` / `wait_couple`（**无跳转命令**），步进语义表见
   `plan_layer_roadmap.md` §2.2/§2.3（含 **R10** 的连挂侧强制步进）；
   原 Q17 三问已封闭（跳转 = 不做；步进表 = 已定；失效显示 = 可见+原因）。
3. **失效语义** ✅ 已定（Q23-2/3，见上）；**"前往连挂"的搜索与限制** ✅ 已定
   （demo 不做，Q23-7）。
4. **风险登记 R1~R11**（`wagon_centric_data.md` §9）：**R1 已按"拆分"落地**
   （`Wagon.tick` 空实现、demo 不接线 = Q23-9）；**R10 已定并写进语义表**；
   **R11 押到 P3 与遴选实现一起加回归断言**；**R9 因"计划不落盘"降级延后**；
   其余（R2/R3/R4/R5/R6/R7/R8）为长期/纪律类，已标注处置时机。
5. **Q12**（`Consist.wagons` 落盘措辞）——因"计划不落盘"降级为纯措辞问题，不阻塞。

> 📌 **规划阶段已封口**：Q21+Q22+Q23 共 17 项全部有答案，阶段表"被谁卡住"一列已清空；
> **剩余开放项只有"明确延后"与"实现期微决策"两类**，逐条列在
> `docs/plan_layer_roadmap.md` §6.2/§6.3。⇒ **允许进入实现阶段（从 P0 起）。**

### 4.2 ⏸ 明确延后（不阻塞实现）

| 事项 | 原因 |
|---|---|
| **POI 的 O1~O9** | Q22-1：锚点先用现成图要素 ⇒ 移出关键路径 |
| **计划落盘 / 几何锚定** | Q23-6：demo 不落盘 |
| **`Wagon.tick` 接线** | Q23-9：demo 不接线（指针不靠 tick） |
| **`goto_couple` 的目标搜索与限制** | Q23-7：demo 只做最简形态 |
| **显式跳转 / 标签** | Q23-8：demo 不做 |
| **货物/装载** | roadmap #10（已定 demo 不做） |
| **Q12 措辞** | 因"计划不落盘"降级为纯措辞问题 |

### 4.3 🔧 实现期微决策（无需再开会，实现时按 `plan_layer_roadmap.md` 口径定并回填）

锚点/失效条目的颜色与文案 · `goto_couple` 的"前端/后端"如何选 · 等待提示样式 ·
`model/segments.py` 的对外 API 命名 · `plan.py` 与 `dispatch.py` 的模块边界。

### 4.4 ⏳ 2026-09-15 文档快照新登记的待拍板项（5 项）

| # | 待定项 | 我的建议 | 触发阶段 |
|---|---|---|---|
| 1 | **P3 是否加临时调试入口**（`Shift+右键` 追加 `goto` 条目，便于人工看到"计划在跑"；P5 移除） | **建议加**（否则 P5 之前游戏里看不到任何变化） | P3 |
| 2 | **节点合并（DELETE 二度节点）若删掉的是计划引用的锚点** | 与删除同构：**拒绝**（更符合 Q21-3）；否则退化为"永久失效 + 标红" | P4 |
| 3 | **计划的最小编辑能力**：Q22-4 说"不做面板 GUI"，但"删除当前条目 / 清空计划"要不要 | **建议要**（编辑态 `Delete` 删当前条目、`Ctrl+Delete` 清空） | P5 |
| 4 | **`wait_couple` 是否要求"停在站台内"**（px-patch 要求；我们还没有站台实体） | **建议不要求**（只要求零速原地 + HUD 标注"等待连挂"） | P7 |
| 5 | **P9 两件**：许可协议选型（MIT/Apache-2.0/GPL-3.0/…）；`manual_track.geojson`（你的实时存档，**已入库**）的去向 | 许可待你定；存档建议收进 `saves/` 标注"示例"，或移出仓库 + `.gitignore` | P9 |

## 5. 外部参考（2026-09-10 用户指定）—— ✅ 三轮调研已完成

> **结论已固化：`docs/research_pxpatch.md`**（来源清单 + 逐条结论 + 3 借鉴 + 3 陷阱
> + 4 条待人工确认；**§5 = 二次调研（源码：等待如何退出）**；
> **§6 = 三轮调研（官方文档逐页取证）**；**§7 = 三轮新带出的 Q18/Q19/Q20**）。
> 下表保留为"来源台账"。

**三轮（文档侧）最值钱的三条**：① **解挂命令自带"两部分各自的调度计划"下拉
（6 个选项，含每个选项"实际生成的调度计划"）** ⇒ **文档级铁证印证我们的 Q16**
（解挂命令声明后段计划来源）；② **连挂侧的"合并后执行谁的计划"也有显式规则**
（默认用**前往方**的计划、可勾选切换）⇒ 我们的**遴选 + 不交割**路线必须写清，
否则实现时容易顺手照抄成"合并即继承对方计划"；③ **玩家可见语义全用序号**
（课本就叫「总是跳至第二项」）+ **失败完全不提示且手册自陈是缺陷** ⇒ 支持
Q17-1 选"下标"、Q17-3 选"可见+带原因"。

**三 条可借鉴**：① **"等待"是命令的执行语义**（px-patch 的 `OT_WAIT_COUPLE` 无
suspended 状态，只是每帧执行时不产生位移）——印证我们的 Q13；**但连挂交割指针后必须
显式越过该条 wait 命令**，否则接手方重新进入等待（我们记为新风险 **R10**）。
② **"解挂"是一条命令 + 一个"后段计划从哪来"的策略枚举**，而不是"产生两列车的动作"
（→ 我们的 **Q16**，✅ 已拍板方向；**三轮文档已给出他们那份枚举的完整 6 项**，见 §6.1）。③ **"执行另一个计划"用双指针**（当前表 + 自己的表与
回归格号）——将来做"计划嵌套/临时借用"时的零成本形态。

**三 条陷阱**：① **参照物本身是"计划归编组"**（单一 primary 车头、无多控制车概念）
⇒ **不能拿它论证/否定我们的多控制车遴选**；它的唯一仲裁是"谁连挂谁"，且**平局刻意
给在位者**（我们记为 **R11**）。② **他们从不"执行期跳过失效命令"**（只有每 20 天
巡检 + 新闻提示）⇒ 我们的"跳过该条、计划保留"是**自己的延伸**，Q11 的出处已就地更正。
③ **解挂后两段共享同一可变计划对象**（`AddToShared`）——与审计 B1 同型风险，**照抄
会与"计划归控制车"直接冲突**。

**4 条待确认（三轮调研后的状态）**：① Q11 出处更正 ✅ 已改（"执行期跳过"是**我们的
延伸**，他们只有巡检 + 新闻）；② **"等待连挂"如何退出** ✅ 已答（→ **R10 已定**：
他们靠 `Couple()` 末尾强制越格，我们连挂侧照搬、解挂侧由 Q16 消解）；
③ **`priority`/id 遴选在连挂后是否易主** → ⏸ 押到任务 4 加回归断言（**R11**）；
④ **站台建模路线差异** ✅ 已写进 `poi.md` §8，三轮又补了"站台带长度"（**O9**）。

| 来源 | 地址 | 说明 |
|---|---|---|
| **OpenTTD-PXPatch 官方文档（中文）** | `pulsexlb.github.io/OpenTTD-PXPatch-Docs/zh/` | 用户指定：**专门加入连挂/解挂功能的 OpenTTD 分支**；开发者是用户认识且高度认可的人。✅ **三轮：10 页逐页读完**（解挂 1.1/1.2/1.3、对接 2.1/2.2、模块化机场、自定义调度计划表〔正文仍 WIP〕、首页/目录）——**玩家可见语义的唯一来源** |
| **DeepWiki 摘要** | [deepwiki.com/pulsexlb/OpenTTD-patches](https://deepwiki.com/pulsexlb/OpenTTD-patches) | ✅ 已读（Orders/Timetables/Scheduled Dispatch 章） |
| **源码** | `github.com/pulsexlb/OpenTTD-patches`（分支 `px-patch`） | ✅ 已读（`order_type.h` / `order_cmd.cpp` / `train_cmd.cpp` / `vehicle_base.h` / `train.h` 等）——**关键结论多由此得出** |
| JGRPP | OpenTTD JGRPP 文档 | 未直接引用（px-patch 行为均已由自身源码验证） |

**我们想从里面学到什么（调研问题清单——请逐条给答案 + 出处）**：
1. **连挂/解挂的命令与计划语义**：「前往…并等待 couple」如何被拆成命令？事件如何驱动
   指令指针步进？"挂起"在数据上如何表达（是否真的只是"停在等待命令上"）？
2. **多机车/多控制单元的控制权归属与冲突解决**：我们暂定"`priority` 大者胜 → `wagon_id`
   取小"，他们有没有更好的做法（或反例告诉我们这个简化会在哪里崩）？
3. **调度计划的数据类型与编辑交互**：条目结构、跳转/条件、循环、非法条目（invalid orders）
   的处理与提示。
4. **装货的瞬时结算 + CargoDist 的分配模型**（我们缓办，但要知道设计边界，避免将来返工）。
5. **站台/停靠点如何表达**：对照我们的「有向点 / 路段 / 建筑 = POI 集合」。
6. **他们把什么当原子单位**：列车（编组）vs 车厢——他们为支持连挂/解挂改了哪些数据结构？
7. **自动驾驶的层级划分**：计划 / 寻路 / 信号授权 三层如何分工（对照我们的 Q7 单向管道）。

⚠ **参考 ≠ 照抄**（用户明确口径）：**不得照抄任何一个同类游戏**，调研结论要经过研讨
再决定是否采纳；采纳的写进 `wagon_centric_data.md` / `poi.md` / 未来的计划层文档，
并把一行摘要回填到本文 §2/§4。

## 6. 文档快照（2026-09-15；索引 + 状态）

> 本节是**文档级快照**：每份文档"管什么、现在什么状态"。对应提交 `ef78232`。
> 判据：**权威规格**（改代码必须同步）/ **台账**（决策与进度）/ **历史**（只作背景）。

| 文档 | 管什么（权威范围） | 状态 |
|---|---|---|
| `CLAUDE.md` | **agent 入口**：分层规则、图拓扑不变量、编辑器/信号/存档约定、键位表、计划层定案摘要 | ✅ 最新（含 P0~P3a 进度） |
| `docs/progress_snapshot.md` | **续期入口 + 研讨台账**（本文）：已拍板 Q1~Q23、封口状态、代码状态、文档索引 | ✅ 本次刷新 |
| `docs/plan_layer_roadmap.md` | **计划层实施路线 P0~P9**：阶段表、**§3.1 待做阶段实现要点与验收标准**、§3.2 已完成记录、§4 技术设计、§6 封口/延后/登记 | ✅ 最新（P3~P9 要点本次补齐） |
| `docs/path_level_plan.md` | Q21 提案原文、**代码事实核对（含实测）**、Q22 问卷与结论 | ✅ 定稿（方向已定） |
| `docs/wagon_centric_data.md` | 车厢中心数据结构：Q1~Q23 决策、§6 逐字段归属（四桶）、§9 风险登记 R1~R11 | ✅ 结论已定稿；§9 为长期台账 |
| `docs/poi.md` | POI 三类建模、建筑=POI 集合、O1~O9 待研讨 | ⏸ **已移出关键路径**（Q22-1） |
| `docs/research_pxpatch.md` | 外部调研三轮（源码 / 等待退出 / 官方文档）+ 可借鉴与陷阱 + Q18/Q19/Q20 | ✅ 已完成（参考，不照抄） |
| `docs/roadmap.md` | **项目总账** #1~#10 + 当前重心 + 已知限制与未来方向 | ✅ 最新（#7 指向新路线图） |
| `docs/editor.md` | **编辑器权威规格**（Steps 0–6 + 吸附 + 空间索引） | ✅ 稳定；P5 加计划编辑键位后需回填 |
| `docs/train_control.md` | **信号系统权威规格**（固定闭塞 / One-Way PBS / 近远场 / 授权） | ✅ 稳定 |
| `docs/consist_ui.md` | **连挂/解挂 UI 规格**（车钩悬停 / 键位 / §5.5 信号豁免） | ✅ 稳定；P7 计划内连挂落地后需回填 |
| `docs/session_persistence.md` | **存档规格**（几何锚定 / 存与不存 / 容错） | ✅ 稳定；**计划不落盘**（Q23-6）⇒ 无需改 |
| `docs/kinematics.md` · `docs/physics.md` · `docs/pathfinding_fixes.md` | 历史设计与修复记录（背景，非现行规格） | 🗄 历史（P9 可标注/归档） |
| `README.md` | 对外门面 | ⚠ **当前 0 字节**，P9 必写 |

**检查结论（本次快照时点）**
1. **代码与文档一致**：P0~P3a 均有阶段记录 + 可复现的测试证据；
2. ⚠ **后面各阶段此前只有一行摘要**（无验收标准）——本次已补 `plan_layer_roadmap.md` **§3.1**
   （P3~P9 逐阶段的**实现要点 + 验收标准 + 待定项**）；
3. 新登记 **5 项待拍板**（本文 §4.4）；
4. 仓库未发现个人绝对路径残留；`.gitignore` 已覆盖 `.venv/`、`__pycache__/`、`.DS_Store`、`.claude/`；
5. **未发现文档与代码相互矛盾之处**——唯一已知偏差是
   `simple_segment_from_endpoint` 的**急折盲区**，已按用户裁决"不修"并在代码处就地登记。

## 7. 接手建议（新会话第一步）

1. **读本文**（§1 当前状态 / §3 代码状态 / §4 封口与待拍板 / §6 文档快照）
   → 2. **`docs/plan_layer_roadmap.md` §3 阶段表 + §3.1 待做阶段实现要点与验收标准
   + §3.2 已完成记录**（决定"现在该做哪一步、做完怎么验收"）
   → 3. 要看形态与设计取舍时读 **`docs/path_level_plan.md`**（Q21 提案 + 代码核对
   + Q22 结论）与 **`docs/wagon_centric_data.md`**（Q1~Q23 决策、§6 逐字段归属、§9 风险台账）
   → 4. 动手前的硬约束看 **`CLAUDE.md`「计划层定案」** 与本文 §4.0/§4.4
   → 5. 每完成一个阶段：**跑全套回归**（`tests/test_*.py` 直跑）→ **回填 §3 与本路线图
   §3.2** → 触及连挂/解挂/折返带**请用户人工复测**。
   ※ 历史参考：`docs/research_pxpatch.md`（外部调研三轮）、`docs/poi.md`（已移出关键路径）。
