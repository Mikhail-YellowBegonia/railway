# 以车厢为中心的数据结构（研讨稿）

> **状态：研讨中，未定稿，不实现。** 本文只记录设想与待研讨问题。
> **进度（2026-09-10）**：**已拍板** Q1（持久化边界 = 方案 A：域数据归车厢、
> 位置/速度归"运行期派生"、不改运动学）、Q2（四种控制/动力组合都支持；**控制是
> 决定性属性，动力只是物理参数**）、**Q3（"只读"是设计准则、不做强制；"自力更新"
> = 只有 wagon 类下属逻辑能改 wagon 字段；装载因此反过来：车厢凭信号从站台"抢"货）**、
> **Q4（克隆 = 新车：缺省值、不继承状态）**、**Q5（`priority` 相同 → `wagon_id`
> 取小者胜）**、Q6（无控制车的段静止；**计划只有控制车才能持有**）、
> **Q7（单向管道：调度计划 → 寻路需求 → 寻路结果 → 预约路径 → 物理模拟 → 位移；
> 遴选每帧进行）**、Q8（**POI 最小单元 = "有向点"**：1 node + 1 edge，edge 只给方向
> 不给长度；完整调研未开始）、Q11（悬空引用 → 跳过执行但保留计划）、
> **Q13（事件 = 直接调用车厢的对应方法；"挂起"不存在——等待只是命令内容）**、
> **Q14（指针步进 = 列车到达后直接调用控制车的方法）**。
> **已执行**（T0~T2，见 §8）：Q9（`data_log` 迁入车厢）、Q10（删死代码）。
> **仍待研讨**：Q12 与 **§9 风险登记 R1~R9**。
> **§6 是逐字段归属表草稿**（不求全，接受持续修改与扩充）。
> 来源：用户 2026-09-10 评审「编组数据结构审计结论」（`docs/roadmap.md` #9 第 1 项）
> 后提出——判断"现在的数据结构不太稳固，建议重新思考"。
> 关联：`docs/roadmap.md` 待办 #9（编组原子性/元数据交割复核）、#7（计划式自动驾驶）；
> 本文是 **#9 第 3 项「新增调度计划相关数据类型」的前置输入**。

---

## 1. 为什么要重想（动机）

现有数据结构把"车厢（Car）"和"列车（Consist/TrainEntity）"的职责混在一起：

- `WagonConfig` 是**可变** dataclass，且混装了"定死的物理属性"与"会变的载货/任务"。
- `Consist` 既持有**车厢级**数据（`data_log`：按 `wagon_id` 归属的数据包），
  又持有**编组级**状态（`velocity`/`acceleration`，实测为死字段）。
- 全部**运行期状态**（`v`/`occupancy`/`route`/`goal`/`authority_remaining`/
  `controller`/`v_target`/`couple_approach_partner`）都挂在 `TrainEntity` 上，
  并被 `model/session.py` 持久化——**编组变化（解挂/连挂）时这些全部被丢弃**
  （见 `roadmap.md` #9-B3 实测）。

这正是 G 阶段当初预设的风险（"以车厢为最小单位"时的元数据/调度交割）在数据结构
层面的表现。**本稿的目标是把职责重新划清，而不是打补丁。**

## 2. 设计设想（用户 2026-09-10 提出）

### 2.1 车厢的根本属性是 `have_control`

用 `have_control` 区分**控制车**与**非控制车**。（现状代码没有这个概念，只有
`WagonConfig.is_powered`（= `P_rated is not None`）兼任"是不是机车"，两者语义
不同——见 §4.2 Q2。）

### 2.2 车厢属性只读，只能自力更新

- **物理属性不可写入**：车厢的（空载）重量、马力、性质等**定死**，不接受列车写入。
- **载货量只能自力更新**（近期不实现）：学习 OpenTTD——能逐节车厢看到载货量。
- **调度计划不可写入**：打比方说，**每个控制车都有一个司机，有自己的任务，与列车无关**。

即：**列车只能读车厢，不能写车厢**；车厢要变，只能自己变。

### 2.3 列车编组不储存任何持久化数据，只汇总

- **汇总物理属性**：收集全体车厢的物理属性（质量、功率…），算出自己的可用加速度，
  再运行动力学逻辑。
- **汇总调度计划**：收集全体**控制车**的调度计划，**遴选后执行**（见 §2.4）。
- **按需向车厢发信**：例如装卸货命令（近期不实现）——但这类消息**一过性**，不持久。

> **"持久化数据"的边界已于 2026-09-10 拍板（Q1 = 方案 A）**：本条的"不储存
> 任何持久化数据"应读作"**不拥有任何域数据**"——**域数据**（物理属性 / 载货 /
> 调度计划）一律归**车厢**所有；而**运行期派生状态**（速度、`occupancy` 窗口、
> `controller`、`authority`、由选中计划投影出来的 `route`/`goal`）**编组可以持有**，
> 随时可重算，存档时作为"恢复位置用的快照"照旧落盘。详见 §5 Q1。

### 2.4 多控制车的冲突：先给最简解法（深层问题暂不回答）

一列车有两节以上控制车时，它们会"各执一词"，可能持有不同的调度计划，该执行哪个？
**用户明确表示这是个复杂要命的问题，在想清楚之前先不回答**，先采用最简解法：

> **每个控制车自带一个优先级（不查重）。出现冲突时按优先级采纳；优先级相同的
> 用 id 大小决断。**

（"不查重"= 允许优先级重复，不做唯一性校验。id 用 `wagon_id`；"取小还是取大"
尚未拍板——见 §5 Q5。）

## 3. 这套设想顺手解决了什么（与审计结论对照）

`roadmap.md` #9 审计列出的 6 条风险，本设想**直接化解其中 5 条**：

| 审计条目 | 现状问题 | 本设想为何化解 |
|---|---|---|
| B1 车厢是共享可变别名 | 同一 `WagonConfig` 对象跨编组共享，就地改字段会串味 | 物理属性**只读**后，共享别名从"隐患"变成"正确的同一性"；会变的状态（载货量）本就该是同一节车厢的同一份状态，**共享是对的** |
| B2 `physics` 交接无规则 | 解挂共享父的、连挂取前车的，后车被静默丢弃 | 物理属性归属车厢后，`TrainPhysics` 只剩"公式集"（无状态），不存在"谁的物理"问题 |
| B3 列车级元数据无交割通道 | `goal`/`v_target`/`authority`/controller 在编组变化时全部丢弃 | **计划归控制车所有**（"司机与列车无关"）→ 编组变化时计划不走编组，天然不丢；编组每帧重新汇总遴选 |
| B4 `Consist.velocity/acceleration` 死字段 | 与 `TrainState.v` 双重真源、从不更新 | 直接删除（编组只汇总，不存状态） |
| B6 `data_log` 归属规则怪癖 | 非车厢归属的包被静默扔到后段；`merge` 不去重 | 车厢级数据**移入车厢自身**（`WagonDataPacket` 不再"托管于列车编组"）→ 拆分/归并记账整体消失，怪癖随之消失 |
| **B5 `couple_with(self)` 无自反断言** | 实测产生重复车厢、总质量翻倍 | **不化解**——仍需在模型层补断言（本稿不修，记在案） |

另外两点现状事实与本设想一致，可直接沿用：

- **汇总式读取已有先例**：`Consist.total_mass` / `total_length` 已是 property，
  `RealisticElectric` 已按车厢遍历累加牵引力——**"动力叠加"（多机车共同出力）
  与"计划单一"（只采纳一辆控制车的计划）并不矛盾**，是两个正交的量：
  牵引按全部动力车求和，计划按优先级取一个。
- **`wagon_id` 已稳定**（uuid 直存、跨存档保持、`reversed_config` 也保留），
  所以"用 id 决断"在跨存档场景下是确定性的。

## 4. 与现状的差距（代码事实，2026-09-10 核对）

> ⚠ **本节是 T0~T2 落地前的快照**（保留作背景）。T2 后已变的部分：
> `have_control`/`priority` **已加**（惰性、无消费点）、`data_log` **已迁入车厢**、
> `Consist.velocity`/`acceleration` 与 `reversed_*` **已删除**、physics **已去身份**。
> 逐字段实况见 §6（表内已标 ✅）与 §8「执行状态」。

| 设想 | 现状 | 差距性质 |
|---|---|---|
| `have_control` 字段 | **不存在**；只有 `is_powered`（`P_rated is not None`），被物理层（牵引）、渲染（`Loco`/`Coach`）、解挂提示（"单节无动力车厢无法自行行驶"）复用 | 需新增字段并把三处用途拆开 |
| 车厢属性只读 | `WagonConfig` 是**普通可变** dataclass；`__post_init__` 会就地写 `coupler_2_pos` | 需改只读（frozen 或私有字段+只读 property + 窄更新接口） |
| 载货量自力更新 | **不存在**（近期不实现，符合预期） | 新增时须定"谁触发更新"（车厢自己没有 tick） |
| 调度计划归车厢 | **不存在**；计划层整体未做（roadmap #7） | 待 #7 研讨；本稿只定"归属权" |
| 编组只汇总、不存持久化数据 | `Consist` 持有 `velocity`/`acceleration`（死字段）与 `data_log`（车厢级数据存编组）；`TrainEntity`/`TrainState` 持有并持久化全部运行期状态 | 见 §5 Q1（**最大分歧点**） |
| 向车厢发一过性命令 | 无此通道（现有"写入"只有 `TrainPhysics` 读、`session` 序列化读） | 待建（装卸货近期不做） |
| 多控制车优先级冲突 | 无优先级概念；多机车只体现为**牵引力叠加** | 需新增 `priority` 字段 + 遴选规则；**牵引叠加保持不变** |

**已确认的死代码（与本稿相关）**：`Consist.reversed_consist()` 与
`WagonConfig.reversed_config()` 自 2026-09 拍板"折返只换前进方向、不反转编组"
后**已无任何调用者**（只有自引用与文档提及）。它们与本设想也冲突（镜像会造出
同一 `wagon_id` 的第二个对象 = 身份重复），去向见 §5 Q10。

## 5. 待研讨问题（**Q1/Q2/Q6/Q8 已拍板**；其余仍**未做结论**，定稿前不要据此写实现）

**Q1 ✅ 已拍板（2026-09-10）：采用方案 A —— 域数据归车厢，位置/速度归"运行期派生"。**

- **域数据 → 车厢拥有**：物理属性（空载重量/马力/性质）、载货量、**调度计划**。
  随车厢走（解挂带走、连挂随车、存档逐车厢落盘）。
- **运行期派生状态 → 编组持有**：`TrainState.s` / `occupancy` 窗口 / `v` /
  `controller` / `authority_remaining` / `v_target`，以及**由选中计划投影出来的
  `route`/`goal`**。它们随时可由"车厢域数据 + 轨道 + 运行期输入"重算，**编组只是
  当前运行期的宿主**；存档时仍写快照（`docs/session_persistence.md` 的位置还原机制
  不变）。
- **不做**方案 B：运动学真值**不下沉**到车厢 → `RigidWagonKinematics` 刚体链
  （单一车头弧长 + 逐节链式求解）与 `session` 的位置还原机制**保持现状，不重写**。

**因此得到的判定规则（新字段先问这一句）**：
> 这个字段是**域数据**（描述这节车厢本身：物理、载货、任务 → 车厢拥有、随车厢走）
> 还是**运行期派生**（描述"此刻这列编组怎么跑"：位置、速度、授权、计划投影 →
> 编组持有、可随时重算/丢弃）？
> —— 前者进车厢且只读（载货量等由车厢自力更新）；后者进编组，**不允许**被当作
> 域数据长期保存或跨编组交割。

**方案 A 的直接后果（供任务 3/4 落地时对照）**：
1. **审计 B3 的消除方式明确化**：计划本身在控制车上；编组的 `route`/`goal` 只是
   选中计划的**投影**。解挂/连挂时投影被丢弃**不是丢数据**——新的编组会按优先级
   重新遴选控制车、重新投影（这正是"司机与列车无关"）。因此 `session.py` 需要
   逐车厢落盘域数据（计划；物理属性已在存），而运行期快照照旧。
2. **不必碰运动学与位置还原**：`TrainState.s`/`occupancy`/`RigidWagonKinematics`/
   `session` 的位置机制按原样保留 → 改动面从"重写运动学"缩小到"**划分字段归属 +
   给车厢加域数据与只读约束**"。
3. **`Consist.velocity`/`acceleration` 判定为违规**（编组持有域数据性质的字段且从不
   更新）→ 按本方案应删除。
4. **`data_log`/`WagonDataPacket` 判定为违规**（车厢级数据却托管于编组）→ 移入车厢
   自身（Q9）。
5. 解挂/连挂的解算**简化**：不再需要"编组级元数据交割"这一整类逻辑，只需要
   "重新遴选 + 重新投影"。

**Q2 ✅ 已拍板（2026-09-10）：四种组合原则上都要支持；控制与动力不在一个层面上。**

- **支持四种**：①控制+动力（常规机车，最常用）②控制无动力（驾驶拖车）
  ③有动力无控制（被推挽的补机/动车组拖车）④都无（普通车厢，最常用）。
- **控制是车厢的决定性属性**（"比较重要"）→ `have_control` 是车厢的一级属性，
  与身份（`wagon_id`）同级，**决定"这节车厢能不能持有并执行任务"**。
- **动力只是物理参数的一部分**（"无足轻重"）→ `P_rated` 与 `mass`/`length`/`bogies`
  同级，归入**物理属性**，只影响动力学汇总，不决定任何"是谁/能做什么"。
- 推论（沿用）：**牵引照旧按全部动力车叠加**；**计划只从控制车中遴选**。
- 落地含义：现有 `is_powered`（= `P_rated is not None`）**不再承担"机车"的语义**，
  渲染的 `Loco`/`Coach`、解挂提示等三处用途要改判为按 `have_control` 或按动力
  分别表达（见 §7 表）。

**Q3 ✅ 已拍板（2026-09-10）："只读"是**设计准则**，不是强制逻辑。**

- 用户原文要点：**"只读"不是强制逻辑，是设计准则，算是一种防御性措施。**
  ⇒ **不做** frozen dataclass / 私有字段 / 访问控制（**T3 里"只读冻结"一项撤销**）。
  纪律靠文档（T0 的模块 docstring 规则）+ code review。
- **"自力更新"的定义**（用户原文要点）：**wagon 自有的字段，只能由 wagon 类下属的
  逻辑来更新**——外部（列车 / 编组 / 调度计划）不得直接写车厢字段，只能**调用车厢
  自己的方法**。
- **因此装载货物的行为也反过来**（用户原文）：车厢**从列车（调度计划）获得装载的
  信号**，然后**从站台上"抢"货**——而**不是**站台给车厢"装"货。用户明确说
  **"这很怪，但符合 wagon_centric 的设计理念"**。
- 直接后果（记录）：
  - A 桶字段的写入 API 形态 = **车厢类的方法**（未来的形如
    `wagon.take_cargo_from(platform)`），调用方只负责发信号，**不把数据塞进车厢**。
  - "谁在什么时刻调用车厢方法"仍未定 → 见 Q13 与本稿 §9 的风险登记 **R1/R2**。

**Q4 ✅ 已拍板（2026-09-10）：克隆 = 新车，不继承原状态。**

- 用户原文要点：**克隆出的列车相当于玩家新购买的**（如果确有经济系统的话），
  因此**自然是空车、新车：填缺省值，不继承原状态**。
- 含义：新 `wagon_id`（新身份）、空载、无计划、`have_control`/`priority` 按工厂默认
  或玩家购买配置。
- ⇒ 此前担心的"克隆时共享可变状态串味"**不再是问题**（克隆根本不共享）。
- 附带事实：**本项目目前没有经济/购买系统**（roadmap 也没有该条目），"克隆"入口本身
  也未定义；本决策先作为**语义占位**（§9 R7）。

**Q5 ✅ 已拍板（2026-09-10）：优先级相同 → `wagon_id`（uuid4 字符串）取小者胜。**

- 遴选规则因此完全确定（**不查重**，允许 `priority` 重复）：
  **先比 `priority`（大者胜），相同再比 `wagon_id`（小者胜）**。
- **尚未实现**（当前无消费点；实现属任务 3/4，且不得提前接入——见 §8 T2 护栏）。

**Q6 ✅ 已拍板（2026-09-10）：无控制车的段静止；计划只有控制车才能持有。**

- **解挂后无控制车的段 = 静止**（无计划，也就没有可执行的指令）。
- **它不可能"保留"计划**——因为**调度计划只有控制车才能持有**；这不只是取舍，
  而是 Q1/Q2 的直接推论（计划是域数据，只属于控制车）。
- 推论（**2026-09-10 用户确认"基本正确"，并按 OpenTTD decouple 分支补充如下**）：
  连挂时"落选计划"**不存在丢弃/搬移问题**——计划始终留在它那节控制车上
  （"司机与列车无关"），遴选只决定"此刻执行谁的"。**但"挂起"确实存在**，它的
  形态不是"把计划搬走/标记删除"，而是"**指令指针停在一条等待命令上**"。

**Q6 补充（2026-09-10 用户给出参照：OpenTTD 的 decouple 分支版本）**：
- 该分支里有一种调度计划叫「**前往…并等待 couple**」，实际被拆成原版的
  「前往…」+ 新增的「**等待 couple**」；**执行"等待"命令的车就处在挂起状态**。
- 这依赖**事件信号**。低优先级控制车的写法（用户原文示例）：
  1. 前往 A 点
  2. 等待连挂
  3. 空命令（解挂后跳转至此）
  4. 前往 B 点
  列车在 A 点停好时**位于命令 2**，然后挂车、**失去控制权**；过一段时间列车被
  解挂、**控制车收回控制权**，它**仍停在命令 2**——这就需要"**连挂/解挂**"事件
  发出信号，促使控制车**步进到命令 3**，进而执行后续命令。
- **优先级高的控制车没有这个顾虑**：如果它一直持有控制权，顺着写就行。
- ⇒ **由此修订结论**：计划不是"一份意图"，而是"**有序命令列表 + 指令指针**"；
  需要"**等待某事件**"的命令类型；控制权的得与失要靠**事件驱动步进**。
- ⚠ **但不要把它建模成"挂起状态字段"**——见 Q13：用户明确"**没有'挂起'标记**"。

**Q11 ✅ 已拍板（2026-09-10）：悬空引用 → 该条计划跳过不执行，但仍存在。**

参照 OpenTTD 的「**非法的调度计划**」（invalid orders）：引用失效（POI/边被删）
时**不删除计划、不报错中断**，只是**跳过执行**该条，计划本身保留在控制车上，
玩家之后修复引用即可恢复。**因此计划的引用完整性检查是"执行期跳过"而非"编辑期
拒绝"**——这与本项目 DELETE 保护的做法（拒绝删除被列车占用的路段）**策略不同**，
但两者可以并存：DELETE 保护防的是"运行中的列车崩溃"，计划计划层的悬空引用只
影响"这条命令能不能执行"。

**Q13 ✅ 已拍板（2026-09-10）：事件 = 直接调用车厢的对应方法；"挂起"不存在。**

- **送达机制**（用户原文要点）：**事件是从编组发向车厢的，但我们将其解释为直接
  调用车厢的对应方法**——不做事件总线/队列，就是方法调用（与 Q3"外部只能调用车厢
  方法"一致）。
- **"挂起"不存在**（用户原文要点）：**没有"挂起"标记，指令的执行与读取不会停止，
  只是有些指令的内容就包含"等待…"，看起来像是挂起，其实在跑循环。**
  ⇒ 计划的数据类型里**不要**加 `suspended`/`paused` 之类的状态字段；"等待"只是
  一种**命令内容**。
- **指令指针归谁** ✅ **属于控制车自己（车厢级）**（与我此前的倾向一致）。
- **事件送达的范例：解挂时间线**（用户给出，作为实现顺序的依据）：
  1. 列车 C 拿到了解挂的那条指令并执行
  2. 生成了列车 A、列车 B
  3. **列车 C 将信号发给列车 A、列车 B**（= 直接调用它们的车厢方法）
  4. **销毁列车 C**
  5. 列车 A、列车 B 独立运行
  ⇒ 注意顺序：**先通知、后销毁**；通知发生在旧实体仍可访问车厢对象时（车厢对象
  本来就共享，数据上天然成立，但代码顺序必须照此写）。
- ⚠ **未解决的硬问题**（用户明确提出，登记为 §9 **R1**）：**车厢没有 tick，且无法
  避免竞态**，于是只能"**依次轮询停靠在一个车站内的所有列车的所有车厢**"——
  用户评价：**性能上不是问题，逻辑上很成问题**。

**Q14 ✅ 已拍板（2026-09-10，用户追加的问题）：计划指针如何步进。**
问题：指针在控制车（车厢级），但计划由编组执行，而**控制车没有能力自己更新**（没有
tick）——它怎么步进？
用户给出的答案（原文要点）：**只能是列车到达目标之后向控制车发一个信号**；结合
"只读"的需求，**应该还是直接调用控制车的对应方法**。
⇒ 与 Q13 同一条机制（方法调用）。**实现时必须满足两条不变量**（本稿补充，供任务 4
对照）：
1. **单一步进者**：同一时刻只有"当前持有控制权的那列车"能步进该指针——否则解挂出的
   A、B 两段会同时步进、跳命令。
2. **事件一次性/幂等**：到达事件每"到站"只触发一次（否则一帧内多步）。现状已有同类
   模式可复用：`GameLoop.run()` 每帧用 `moving_before`/`just_stopped` 检出"刚停车"
   的列车并只触发一次（连挂的自动对接就挂在那个事件上，见
   `controller/game_loop.py::_auto_couple_on_stop`）。

**Q7 ✅ 已拍板（2026-09-10）：数据流是单向管道，遴选每帧进行。**

- 用户原文要点：**从高到低显然有这样的顺序**：
  **调度计划 → 寻路需求 → 寻路结果 → 预约路径 → 物理模拟 → 位移**；
  **后者消费前者，一路向下，比较干净**。**遴选如果没有太大性能压力，最好每帧进行。**
- 与现有实现的对照（**现状已符合该顺序，只缺最上游那一段**）：

  | 管道层 | 现有实现 |
  |---|---|
  | 调度计划 | **不存在**（任务 3/4 的正题） |
  | 寻路需求 | `GameLoop._issue_goal_order` → `find_path_from_point` |
  | 寻路结果 | `TrainEntity.assign_route(route, remaining_to_goal, goal)` |
  | 预约路径 | `TrainDispatcher.tick` → `BlockManager.reserve_path` → `authority_remaining` |
  | 物理模拟 | `TrainEntity.update` → `TrainPhysics.compute_acceleration` + `BrakingController` |
  | 位移 | `TrainState.occupancy.s` 推进（`state.s += v·dt` 语义） |

- 这条同时把"B 桶运行期派生"的**顺序**显式化了：`route`/`goal` 属"寻路结果"层，
  预约属 BlockManager，二者都**不得反向影响**上游（计划层只读它们的产物）。
- "遴选每帧"的确定性由 Q5 的比较键保证（`priority` 大者胜 → `wagon_id` 小者胜），
  不存在"结果随遍历顺序漂移"的问题。

**Q8 ✅ 已拍板最小单元（2026-09-10；**尚需完整调研**）：POI 的最小单元是"有向点"。**

用户给出（原文要点）：**POI 最小的单元是"有向点"，即 1 个 node + 1 条 edge，
从 edge 处指向 node；其中 edge 只提供方向、不提供长度**。"有向点"在 GIS 视角上
是一种**带额外属性的点要素**。这样它就能代表诸如"**站台的末端**"、
"**有方向的停车点**"等地理事物。

- **性质**：这是**方向性锚点**，不是线段（edge 不贡献长度，只回答"朝哪边"）。
- **能力**：能表达现状 `goal=(edge_id, t, direction)` 表达不了的东西——尤其是
  **"有方向的停车点"**（进站方向、停车位置、站台末端）。
- **注意**：**POI 尚未经过任何调研**，本定义只是最小单元的种子；任务 2 仍是
  "构思 POI 系统"，仍需完整研讨（可扩展属性、放置交互、与信号/block 的关系、
  与既有 `turn_allowed` 的关系等）。
- **归属（本稿新增的第三类，见 §7）**：POI 属于**轨道/基础设施侧**——挂在 node/edge
  上、随轨道一起编辑与落盘，**既不是车厢域数据，也不是编组运行期状态**。

**Q9 ✅ 已执行（2026-09-10，T2-2）**：`data_log` 已**迁入车厢自身**，容器改名
`WagonDataLog`（编组级容器 `ConsistDataLog` 不再存在），其 docstring"托管于列车
编组"已改；`split`/`merge` 与 `split_at`/`merged_with` 的数据包记账**整体删除**
（现在只切/拼成员列表）。**当前仍无生产者/消费者**（`WagonDataPacket` 全仓零
实例化）——通道已就位、内容为空。落盘形态见 §6.6。

**Q10 ✅ 已执行（2026-09-10，T1-2）**：`Consist.reversed_consist()` 与
`WagonConfig.reversed_config()` **已删除**（死代码 + 会造出同 `wagon_id` 的第二
对象）；"折返只换方向、不反转编组"的语义不受影响（回归
`tests/test_reverse_in_place.py` 通过）。

## 6. 逐字段归属草案（**草稿，不求全，接受持续修改与扩充**）

> ⚠ **本节 §6.2~§6.7 的"现状"行是 2026-09-10 T0~T2 落地前写的**；各表中已落地的
> 行已就地标注 ✅（T1-x / T2-x）。**当前代码实况以标注为准**，未标注 ✅ 的
> "新增/删除候选"仍是计划。

> 依据：Q1 判定规则（域数据 vs 运行期派生）+ Q2/Q6/Q8 的拍板。
> **这是草稿**：字段可以补、可以改判、可以新增桶；**未定稿前不据此写实现**。
> 每行都给出现状的代码位置与判定理由，便于逐条复核。

### 6.1 四个归属桶

| 桶 | 含义 | 生命周期 |
|---|---|---|
| **A 车厢域数据** | 描述"这节车厢是什么/有什么任务" | **随车厢走**（解挂带走、连挂随车、逐车厢落盘）；**只读**（例外：载货量由车厢自力更新） |
| **B 编组运行期派生** | 描述"此刻这列编组怎么跑" | **编组持有**，可随时重算/丢弃；存档写快照只为恢复位置；**禁止跨编组交割** |
| **C 基础设施侧** | 挂在轨道（node/edge）上的地理事物 | 只在**基础设施编辑**时变更，随 network 落盘（沿用"只有基础设施编辑才改图拓扑"的不变量） |
| **D 删除 / 迁出** | 违规或死代码 | 按 Q9/Q10 处理 |

**C 桶是本次新增的第三类**——由 Q8（POI 的最小单元）逼出来的：POI 挂在 node/edge
上、随轨道编辑与落盘，**既不是车厢域数据，也不是编组运行期状态**。原判定规则只有
"域数据 / 运行期派生"两个桶，不足以安放 POI，故补 C 桶（信号其实早已属于 C）。

### 6.2 `WagonConfig`（`model/wagon.py`）

| 字段 | 桶 | 说明 / 理由 |
|---|---|---|
| `wagon_id` | **A**（身份，不可变） | 已在存、跨存档稳定；Q2 下与 `have_control` 并列为一级属性 |
| `length` | **A**（物理，只读） | |
| `mass` | **A**（物理，只读） | 空载质量；载货量另算（未来） |
| `bogies[]` + `BogieConfig`（`geometric_role`/`pos`/`load_share`/`axle_count`） | **A**（物理，只读） | ⚠ 嵌套是可变 list，做只读时要连带处理 |
| `coupler_1_pos` / `coupler_2_pos` | **A**（物理，只读） | ⚠ `coupler_2_pos` 现由 `__post_init__` **就地写** → 只读化时必须改写（Q3） |
| `P_rated` | **A**（物理，只读） | Q2：**降级**为与 `mass` 同级的物理参数，不再承担"是不是机车"的语义 |
| `is_powered`（property） | 保留但**语义降级** | 只回答"有没有牵引功率"；现有三处用途要改判：物理牵引（✓ 保持）、渲染 `Loco`/`Coach`（→ 按 `have_control` 或分列显示）、解挂提示"单节无动力无法自行行驶"（→ 按 `have_control`） |
| **`have_control`（新增）** | **A（决定性属性，只读）** | ✅ **已加（T2-1）**：Q2 控制车标志；与 `wagon_id` 同级；**当前无消费点**（渲染/提示待定稿） |
| **`priority`（新增）** | **A**（只读；仅控制车有意义） | ✅ **已加（T2-1）**：Q4 最简解法；默认值与取值范围待定（Q5）；当前无消费点 |
| **调度计划（新增）** | **A**（域数据；**只有控制车持有**） | Q6 已拍板；数据类型形态是任务 3 的正题（**未做**） |
| **载货量 cargo（新增）** | **A（可变，只能自力更新）** | 近期不做；驱动方式待 Q3（**未做**） |
| **`WagonDataPacket`/`data_log`（迁自 `Consist`）** | **A** | ✅ **已迁（T2-2）**：容器改名 `WagonDataLog`（编组级容器不再存在），其 docstring 已改；拆分/归并记账删除 |
| `reversed_config()` | **D（删除）** | ✅ **已删（T1-2）**：死代码 + 会造出同 `wagon_id` 的第二个对象（破坏身份唯一） |

### 6.3 `Consist`（`model/wagon.py`）

| 字段 | 桶 | 说明 / 理由 |
|---|---|---|
| `wagons`（成员与顺序） | **B**（编组构成，存档快照） | 它描述的是"此刻这列车由哪些车厢按什么顺序组成"，**不描述任何单节车厢**；成员身份指向 A 桶。存档必须保留顺序（否则无法还原编组） |
| `velocity` | **D（删除）** | ✅ **已删（T1-1）**：审计 B4 死字段 + 编组持有域数据性质的字段 |
| `acceleration` | **D（删除）** | ✅ **已删（T1-1）**：同上 |
| `data_log` | **D（迁出 → A）** | ✅ **已迁（T2-2）**：编组上不再有数据包容器 |
| `total_mass` / `total_length`（property） | **B**（只读汇总） | 已是 property ✓，正是"编组只汇总"的范式 |
| `split_at` / `merged_with` | 保留（几何/窗口逻辑） | ✅ **已去掉记账（T2-2）**：只切/拼成员列表，车厢域数据随车厢对象自然归属 |
| `reversed_consist()` | **D（删除）** | ✅ **已删（T1-2）**：Q10 死代码 |
| **`control_cars()`（新增查询）** | **B** | ✅ **已加（T2-3）**：遴选输入（筛选 A 桶 `have_control`）；暂无调用者 |
| **`selected_control_car` / `selected_plan`（新增）** | **B**（运行期标记） | "此刻执行谁的司机"；重算时机待 Q7 |

### 6.4 `TrainState` / `TrainEntity`（`model/train_entity.py`）

| 字段 | 桶 | 说明 / 理由 |
|---|---|---|
| `occupancy`（`occupied`/`occupied_offset`/`s`） | **B**（运行期 + 存档快照） | **Q1 方案 A：位置真值维持编组级，不重写运动学** |
| `occupancy.route` | **B**（**选中计划的投影**） | 解挂/连挂丢投影**不是丢数据**（Q1/Q6：计划仍在控制车上） |
| `remaining_to_goal` / `goal` | **B**（投影） | `goal` 的目标点未来改为 **POI**（Q8）→ 跨桶引用 C |
| `v` | **B** | |
| `consist` | **B**（聚合引用） | |
| `network` | 只读依赖 | |
| `physics`（`TrainPhysics` 实例） | **B**（**无状态公式集**） | ✅ **已去身份（T2-4）**：改为模块级共享 `train_physics.DEFAULT_PHYSICS`，"连挂取前车 physics"的问题从结构上消失（审计 B2 化解） |
| `controller` / `last_a` / `v_target` / `authority_remaining` | **B** | 与 `session.py` 现有"不存 controller/authority"约定一致 |
| `couple_approach_partner` / `split_sibling` | **B**（对象引用，不入存档） | 与现状一致 |
| `kinematics` | **B**（由 `occupancy` + `consist` 构建） | |
| `_stop_before_m` | **B**（指令参数） | |

### 6.5 调度 / 轨道侧

| 对象 | 桶 | 说明 |
|---|---|---|
| `BlockManager._reservations` | **B** | 按对象身份登记；现状已按 `trains` 列表释放 ✓ |
| `Node` / `Edge` | **C** | |
| `SignalTable` | **C** | 已有 `prune_missing` 处理悬空引用 ✓ |
| **POI（新增）** | **C** | Q8 最小单元 = **1 node + 1 edge 的"有向点"**（edge 只给方向不给长度）；可表达"站台末端""有方向的停车点" |
| 未来"站台/装卸点"等 | **C**（挂轨道）或 **A**（挂车厢） | 待任务 2 调研 |

### 6.6 `session.py` 落盘面（对照现状）

| 内容 | 变化 |
|---|---|
| 车厢（逐车厢） | 现状已存物理属性 → **新增**：`have_control` / `priority` / **调度计划** / 载货量 / payload |
| 编组（运行期快照） | 现状已存 `v`/`v_target`/`occupied`/`occupied_offset`/`s`/`route`/`goal`/`remaining_to_goal`/physics 类型标记 → **不变**（Q1 方案 A） |
| 轨道侧 | 现状 network + signals → **新增** POI（未来） |
| 不存 | `controller` / `authority_remaining` / `split_sibling` / `couple_approach partner`（现状已如此 ✓） |

### 6.7 补：由本表新暴露的开放问题

**Q11 POI 的引用完整性（跨桶引用）**：计划（A 桶）引用 POI（C 桶），而 POI 会随
轨道编辑被删除/移动 → 计划会出现悬空引用。信号早有同类问题（靠
`SignalTable.prune_missing` 自查清理，Editor 与 SignalTable 刻意零耦合）。
**✅ 已拍板（2026-09-10）：悬空引用 → 该条计划跳过不执行、但仍存在**（参考
OpenTTD 的「非法的调度计划」），即**执行期跳过**而非**编辑期拒绝**——详见 §5 Q11。

**Q12 编组构成的落盘**：`Consist.wagons` 判为 B 桶（运行期构成）但必须存档——
✅ **措辞已澄清（2026-09-10，T0-2 落地）**：B 桶"不落盘"的准确含义是
"**不把域数据存在编组上**"，而不是"编组不能出现在存档里"；`wagons` 是**成员与
顺序**（身份指向 A 桶），随位置快照一起落盘。规则已写入
`docs/session_persistence.md` §2.1 首条与 `model/wagon.py` 模块 docstring。

## 7. 与 roadmap 任务的衔接

- 本稿是 **#9 第 3 项（新增调度计划相关数据类型）** 的前置输入，但不是它的答案。
  **已拍板**：Q1（持久化边界 = 方案 A）、Q2（四种组合都支持；控制是决定性属性、
  动力只是物理参数）、Q6（无控制车的段静止；计划只有控制车能持有；**并补充：
  计划 = 有序命令列表 + 指令指针，"挂起"= 停在等待命令上，靠连挂/解挂事件步进**）、
  Q8（POI 最小单元 = "有向点"）、Q11（悬空引用 → 跳过执行但保留计划）。
  **仍待研讨**：Q3、Q4、Q5、Q7、Q9 细节、Q10、Q12、**Q13（等待事件命令与事件
  信号送达）**。
- **§6 的逐字段归属表是草稿**（不求全、接受持续修改与扩充）：它就是"任务 3 的第一
  件事"的产出物，把 Q1~Q8 的结论落到了每个具体字段上；**逐条复核这张表 = 讨论
  任务 3 的最短路径**。
- **#9 第 2 项（POI 系统）仍是任务 3 的前置**（Q8）：最小单元已定（有向点 =
  1 node + 1 edge，edge 只给方向不给长度），但**完整调研尚未开始**（属性扩展、
  放置交互、与信号/block/turn_allowed 的关系、引用完整性 Q11）。
- **#7（计划式自动驾驶）** 与本文是同一件事的两面：本文定"数据归属与交割"，
  #7 定"计划层的形态与交互"。
- **在本文定稿前，不要按本稿写实现代码**；审计 B5（`couple_with(self)` 自反断言）
  是独立的小缺口，与本稿是否采纳无关，可单独处理。

## 8. 可"无感落地"的准备工作（**不添加新功能**）——执行提案（2026-09-10）

> 用户诉求："规划的事情太多，看看哪些计划可以无感落地（不添加新功能），比如数据
> 结构等；我们可以先做好，这样要共同考虑的东西变少了。"
> **准入标准（三条全满足才算无感）**：①不新增任何玩家可见功能 ②不改变现有行为
> （14 项回归全过）③减少未来的耦合面（让后续"必须共同考虑"的东西变少）。
> ⚠ **T1/T2 涉及改代码**：T1 属于"修复审计发现"、T2 属于"新增惰性结构"，此前约定
> 是**只汇报不修复** → **执行前需用户点头**。T3 明确列为"现在不做"及原因。

### ✅ 执行状态（2026-09-10：用户批准 T0+T1+T2，已全部落地）

| 档 | 提交 | 验证 |
|---|---|---|
| **T0** 纯注释/文档 | `4919434` | 无行为变化（纯注释） |
| **T1** 删死代码 | `fc6808f` | 全套 14 项回归通过 |
| **T2** 惰性结构准备 | 见本次提交 | 全套 14 项回归通过 ＋ **真实存档往返**：`manual_track.geojson` 的旧格式副本读回后新字段按当时行为推导（控制车⇔有动力）、写出后往返**字节稳定**、physics 全列车共享同一实例、真实存档未被改动；另实测 `data_log` 随车厢跨 解挂/连挂 保持、`couple_with(self)` 被拦 |

**T3 仍未做**（等定稿），清单见本档表格。

### T0 纯文档/注释（零代码风险，可立即做）

| 项 | 内容 | 收益 |
|---|---|---|
| T0-1 | 在 `model/wagon.py`、`model/train_entity.py` 顶部写明**归属规则 + 四桶**，并给每个字段加 A/B 桶注释 | 后续任何人（含未来的自己）改结构前先看到规则，不再依赖记忆/CLAUDE.md |
| T0-2 | `docs/session_persistence.md` 补"车厢域数据 vs 编组运行期快照"的归属规则 | 落盘面与四桶对齐，避免"游戏内容与存档机制脱节" |
| T0-3 | 修本文 Q12 的措辞（`Consist.wagons` = 运行期构成，但随快照落盘） | 消掉一处自相矛盾 |

### T1 删除死代码 / 违规字段（**行为不变**，但属"修复审计发现"→ 需批准）

| 项 | 内容 | 证据（已核实） | 收益 |
|---|---|---|---|
| T1-1 | 删 `Consist.velocity` / `Consist.acceleration` | 全仓零引用（审计 B4） | 消掉与 `TrainState.v` 的"双重真源"与误导命名 |
| T1-2 | 删 `Consist.reversed_consist()` / `WagonConfig.reversed_config()` | 零调用者（Q10；仅自引用 + 文档提及 + 一处测试注释提到名字） | 消掉"镜像造出同 `wagon_id` 第二对象"的身份隐患 |

验证：grep 证据 + 全套 14 项回归。

### T2 惰性结构准备（**加字段 / 挪归属，不接任何消费点 → 行为不变**）

| 项 | 内容 | 为何无感 | 收益 |
|---|---|---|---|
| T2-1 | `WagonConfig` 增 `have_control: bool` / `priority: int`；`create_simple_wagon` 增参数（默认由 `P_rated` 推导）；`session.py` 存取（沿用现有 `w.get(key, 默认)` 模式兼容旧存档）；**先不改任何消费点**（渲染 / 提示 / 物理都不动） | 新字段无人读 → 行为完全不变 | Q2/Q4 的载体就位；"渲染 `Loco`/`Coach`、解挂提示改按 `have_control`"这类可见改动留到定稿 |
| T2-2 | 把 `data_log` / `WagonDataPacket` 从 `Consist` **迁入车厢自身**，删掉 `ConsistDataLog.split/merge` 的记账 | `WagonDataPacket` 全仓从未实例化、`data_log` 恒为空、无任何读取者 | Q9 方向落地；审计 B6 的两条怪癖（非车厢归属包被扔后段、merge 不去重）**直接消失**；"未来计划/货物往哪挂"就此确定 |
| T2-3 | `Consist.control_cars()` 查询（暂无调用者） | 无人调用 | 未来"遴选控制车"的输入面先就位 |
| T2-4 | **physics 去身份**：模块级共享一个默认 `RealisticElectric()`（`game_loop.py` / `session.py` 引用它） | 无状态、参数完全相同 → 数值不变 | 审计 B2（"连挂取前车 physics、后车被静默丢弃"）**从结构上消失**；与"物理参数属车厢"的方向一致 |
| T2-5 | `couple_with(self)` 加自反断言 | 只在 UI 已挡住的非法路径上生效（原为静默产出损坏编组） | 审计 B5 关闭 |

验证：全套 14 项回归（重点 `test_couple_ui.py`、`test_session.py` 的 S→重载往返）
＋**一次真实人工存档往返**（在 `manual_track.geojson` 上按 S、重启读回，确认旧存档
兼容）。

### T3 明确"现在不做"（等定稿，避免返工）

| 项 | 为什么现在不做 |
|---|---|
| ~~`WagonConfig` 冻结 / 只读强制~~ | **❌ 已撤销（2026-09-10，Q3）**：用户明确"只读是**设计准则**、不是强制逻辑"→ **不做** frozen/私有字段/访问控制；纪律靠 T0 的文档规则 + code review |
| 载货量字段 + 自力更新 API | 近期不做（用户已定）；形态已定方向：**车厢自己的方法**（如 `take_cargo_from(platform)`），列车只发信号；细节仍待定（§9 R1/R2） |
| 计划 / 命令列表类型 + 事件信号 | 等任务 3；机制方向已定（Q13/Q14：直接方法调用、"等待"是命令内容而非挂起标记、指针在控制车） |
| POI 类型与放置交互 | 等任务 2 完整调研（现在只有最小单元） |
| `route` / `goal` 的"投影"重构 | 需要计划层先能生成投影，等任务 4；顺序已定（Q7 单向管道） |
| 编组构成的落盘措辞 | 已并入 T0-3 |

### 做完 T0~T2 之后，仍需"共同考虑"的清单

**已随 Q3/Q4/Q5/Q7/Q13/Q14 拍板而消解**：只读强制（不做）、克隆语义（=新车）、
id 决断方向（取小）、遴选时机（每帧）、事件机制（直接调用方法）、指针归属与
步进（控制车 / 到达后调用）、数据流顺序（单向管道）——**从 12 条缩到 3 条**：

1. **POI 完整调研**（任务 2；`goal` 的目标点表达仍缺）。
2. **计划/命令类型的具体形态 + "等待"命令的类型集**（任务 3；机制已定，内容未定）。
3. **§9 风险登记 R1~R9**（其中 R1「车厢没有 tick + 竞态」是最硬的一条）。

而"编组元数据交割"这一整类问题（审计 B1~B6）在这套准备之后**结构性消失**：
计划与载货随车厢（A）、physics 无身份、`data_log` 已在车厢、死字段清空。

## 9. 风险登记（"没想好的部分"——用户 2026-09-10 明确点出这类地方最容易变成风险点）

> 目的：把**已知的未知**集中起来，动手前逐条检查，而不是让它们潜伏在代码里。
> 每条给出：为什么危险 / 现在能不能做 / 什么时候必须想清。

| # | 风险 | 为什么危险 | 何时必须想清 |
|---|---|---|---|
| **R1** | **车厢没有 tick + 无法避免竞态**（用户明确提出） | 装载这类"持续发生"的作业只能靠**依次轮询"停在同一个车站的所有列车的所有车厢"**——用户评价"性能上不是问题，**逻辑上很成问题**"（谁先谁后？轮询中途状态变了怎么办？） | 做"装载/站台作业"之前（近期不做，但会影响计划命令类型的设计） |
| **R2** | **谁有权调用车厢方法、何时调用** | Q3 定了"外部只能调用车厢方法"、Q13 定了"就是直接调用"，但**调用者身份与时机未定**；调用顺序错（如解挂"先销毁后通知"）会让信号丢失 | 任务 4（计划接入列车逻辑） |
| **R3** | **指针步进的单一步进者与幂等**（Q14 补充的两条不变量） | 解挂出的 A/B 两段若同时步进同一指针会**跳命令**；到达事件若一帧触发多次会**多步** | 任务 4 |
| **R4** | **计划/命令类型未定** | "等待…"有多少种？跳转（用户示例的命令 3 空命令）如何表达？非法引用跳过后指针怎么走？ | 任务 3 |
| **R5** | **POI 未调研** | 计划的目标点现在只能表达成几何 `(edge_id, t)`；"站台末端""有方向的停车点"表达不了 | 任务 2（任务 3 的前置） |
| **R6** | **每帧遴选的成本与稳定性** | Q7 要求每帧遴选；比较键已定（priority 大者胜 → `wagon_id` 小者胜）故**结果稳定**，但"每帧重建投影"的实际开销未测 | 任务 4（先测再优化，不必提前优化） |
| **R7** | **"克隆 = 新车"依赖尚不存在的购买/经济系统** | Q4 的语义已定，但**入口（玩家如何克隆）不存在**；若将来引入经济系统，需重新核对"新车默认值"的口径 | 引入购买/克隆入口时 |
| **R8** | **只读不强制 ⇒ 纪律风险** | Q3 明确不做强制，那么"谁写了 A 桶字段"只能靠 review；`WagonConfig` 仍是可变 dataclass，`bogies` 仍是可变 list | 长期：任何改 `model/wagon.py` 的提交 |
| **R9** | **计划/载货的落盘格式未定** | 逐车厢落盘的位置已预留（`session.py` 的车厢条目 + `w.get(默认)` 兼容模式），但**类型未定 → 格式未定**；已发生过"存档与游戏内容脱节"的教训（roadmap #2 已知风险） | 任务 3 定类型时**同步**定格式（勿拖延） |
