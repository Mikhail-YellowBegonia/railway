# 路径级调度计划（**方向已定 Q21；实施路线待 Q22 问卷后出新 roadmap**）

> **性质**：用户 2026-09-10 提出的**根本性架构调整**。**方向已拍板**（见 §4.1：
> 锚点级 + 部分控制点；锚点是目标条目的"路径限定"、比目标级低一层；拒绝编辑被引用要素）。
> 本稿 = ① 提案照录 ② **代码事实核对（含实测）** ③ 两条路线的真实差异 ④ 已定方向
> ⑤ **Q22 关键问卷（5 问）** ⑥ 推进顺序。
> ⏳ **在 Q22 答完、新 roadmap 产出之前：不写实现代码。**
> 相关：`docs/wagon_centric_data.md`（计划层机制 Q13~Q17）、`docs/poi.md`（锚点/路段）、
> `docs/research_pxpatch.md`（外部调研）、`docs/roadmap.md` #7/#9。

> **2026-09-15 后续实施修正**：本稿记录的 Q21/Q22 裁决仍有效，但当时的"锚点解析后
> 得到 route"不能再被解读为执行期持续以 Dijkstra 补全。权威实施口径已转入
> `plan_layer_roadmap.md`：编辑期可解析，确认后冻结完整有向边序列，运行期零寻路；
> 同时先建立严格的拓扑变更防火墙。这一补记避免本历史研讨稿与 demo 目标冲突。

---

## 0. 用户提案（原文摘要）

> **当前实现：启发式调度计划**。我们用目标（如车站 A、车站 B）表示调度计划，列车通过
> 寻路算法实时规划出应走的路径。代表实践主要是 OpenTTD。优点是对图变化高度鲁棒且无需
> 适配。缺点是没有确定性，只取理论最优，玩家要通过复杂的路点、信号、甚至可编程信号机
> 来与 Dijkstra 对抗，让列车走出自己想要的路径。
>
> **预期实现：路径级调度计划**。我们用**目标 + 路径**表示调度计划，其中用
> **simple_segment 的集合**表示路径（铁路网可以被完全分割为有限个 simple_segment，
> 且不重不漏），进而可解析为 edge/node 级别的路径。列车**不再需要实时寻路**，但
> **进路预约和等信号灯都照常进行**。代表实践主要是 NIMBY Rails、Transport Fever 等。
> 优点是高度确定，贴合现实。缺点是高度死板，对图变化极其不耐受，需要做大量适配以免
> 悬空引用。对玩家的规划水平要求变高，定义路径的操作成本变高。

**提案自己点出的三个风险**：① 阻止按原计划实现调度计划；② **彻底架空寻路算法**、
切断"调度计划 → 行车"的消费顺序；③ 在做好调度计划功能之前**无法进行任何测试**。

---

## 1. 代码事实核对（2026-09-10 实测；一条修正了提案前提）

### 1.1 今天已经是"算一次、照着走"——**不是**"每帧寻路"

- 全仓**唯一**跑 Dijkstra 的地方是 `GameLoop._find_route`（`controller/game_loop.py`，
  远场寻路，过滤只用 `SignalTable.passable_topology_only`）；调用者**只有两个**：
  `_issue_path_order`（右键设目的地）与 `_couple_to_hovered`（K 键连挂驶向）。
- `TrainDispatcher.tick` 每帧只做"**预约推进 + 授权边界 + 状态转移**"，
  **不重跑 Dijkstra**（`model/dispatch.py`；Step 5/6 的远场/近场拆分即是为此）。
- 运行期的"路径"**本来就是数据**：`TrainState.occupancy.route: list[DirectedEdge]`，
  由 `TrainEntity.assign_route()` 写入；`session.py` 存档还把它直接落盘。

⇒ **消费端（route → 进路预约 → 授权 → 物理 → 位移）与"路径从哪来"已经完全解耦。**
目标级与路径级的差别只在 **谁生产 route**（Dijkstra 还是计划）。
⇒ 提案风险②"切断消费顺序 / 架空寻路"**在机制上不成立**：route 这一层接口不变。
寻路的角色会从"运行时必需"变成"**计划编辑器的补全器 + 手动指令 + 连挂目标搜索**"
（见 §3），不是消失。

### 1.2 "可完全分割为有限个 simple_segment、不重不漏"——**成立，已用真实存档实测**

`manual_track.geojson`（用户实时存档）：**22 节点 / 24 边**，度数直方图 `{2: 18, 3: 4}`
（**没有死端**）。以"度数 ≠ 2"为切割点切分：

| 段（首→末节点） | 边序列 | 长度 |
|---|---|---|
| 4 → 5 | `[2, 1, 3]` | 400.00 m |
| 9 → 6 | `[6, 5, 4]` | 400.00 m |
| 5 → 6 | `[22, 23, 21, 20]` | 1071.24 m |
| 9 → 4 | `[19, 18, 17, 16]` | 1071.24 m |
| 4 → 5 | `[10, 9, 0, 7, 8]` | 405.29 m |
| 9 → 6 | `[13, 12, 11, 14, 15]` | 405.29 m |

**6 段，恰好覆盖全部 24 条边，不重不漏** ✅（段的两端都是道岔；本图是无死端的闭环）。

道岔处的合法转向：**4 个道岔各 4 个（共 16）** ⇒
**全图"走哪条路"的自由度只有这 4 个点**——路径级计划的**信息量很小**，
贵的是**人机接口**（提案说"定义路径的操作成本高"，此为其量化）。

### 1.3 ⚠ 但"段内可通行"**不自动成立**——分割规则要补一条

`RailNetwork.simple_segment_from_endpoint` 沿"度数 = 2"的节点链行走时**不检查
`turn_allowed`**（纯拓扑遍历）。最小反例（**实测**）：

```python
A(0,0) - M(100,0) - N(100,10)   # M 度数 = 2，M 处约 90° 急折
turn_allowed(M, e1, e2) == False   # 反向也 False（正交被前进半平面排除）
simple_segment_from_endpoint(A) -> (110.0, None, [0, 1])   # 仍当成"一整段"
```

⇒ **正确的切分规则 = 在"度数 ≠ 2"**或**"过境转向不许可"**的节点处都要切**；
切完之后"段"才具备"两端之间有通路"这一性质（路径级计划的基本前提）。
⇒ 现状影响：用户当前存档上这类节点 = **0 个**（实测），所以提案**此刻不咬人**；
但编辑器允许建任意夹角，手工急折会产生这种节点，**必须在"分段/校验"里挡住**。

### 1.4 顺带修正的一处文档错误（纯注释，无行为变化）

`RailNetwork.turn_allowed` 的方法 docstring 仍写"夹角 <= 150°（dot >= 常量）"，
与常量 `TURN_ALLOW_DOT_MIN = 0.0` 和 `>` 的实际语义（**严格 < 90°**）矛盾——
2026-09 改"前进半平面"时只改了模块头注释。**已按实际语义补正**（回归照跑）。

---

## 2. 两条路线的真实差异（差异不在"有没有寻路"）

| | 目标级（现状） | 路径级（提案） |
|---|---|---|
| 确定性来自 | 代价函数 + 图的形状 | **计划本身** |
| 图变化的后果 | 路径自动换一条（可能不是玩家想要的） | 计划**悬空**（可能整条失效） |
| 玩家表达 | 站点列表（+ 路点/信号去"对抗"） | 轨道级行程 |
| route 生产者 | Dijkstra（下单时一次） | 计划 |
| 失效模式 | "列车走了另一条路"（不易察觉） | "列车不动/计划标红"（显眼） |
| 我们的现成对应物 | `_issue_path_order` + `find_path_from_point` | **尚无**（`poi.md` 的"路段"最接近） |

**重要的中间形态（有成功先例）**：Transport Fever 2 的正式模型是"**站点列表 +
路点（waypoint）**"——玩家社群解决"想走特定路线"的常规手段正是加路点
（见其讨论区 "Selecting a specific route with waypoints"），
NIMBY Rails 才是**显式轨道行程**。⇒ 实务上是**三档**：

1. **目标级**（OpenTTD）：只给站点；
2. **锚点级（半确定）**：站点 + 路点/道岔锚点，**锚点之间仍由寻路补全**；
3. **段级（完全确定）**：整串 simple_segment 写死（提案的形态）。

⭐ **我们的 POI 方案已经跨在两条路线之间**：`docs/poi.md` 的三类 POI 里，
"**无向点**"按定义就是 Waypoint（度数 = 2 的节点），"**路段**"按定义就是
`Simple_Segment`。⇒ 把 POI 当"锚点"用就是第 2 档；把段序列显式化就是第 3 档。
**这是本项目不必二选一的结构性原因。**

---

## 3. 建议方案（**2026-09-10 已按用户裁决更新：Q21-1 = B / Q21-2 = 并存且"锚点是更低一层" / Q21-3 = A**）

### 3.0 用户裁决原文（2026-09-10）

> 1. 在粒度上，我们希望做**锚点级 + 部分控制点**，也就是 B 方案。
> 2. 锚点要**与目标级条目并存**。事实上，**它比目标级条目低一层，只有目标条目携带了
>    实际逻辑，锚点序列在解析之后只是一个路径而已。**
> 3. 我**同意拒绝编辑被引用的要素**，图变化适配是复杂且困难的，既然现在不适配，
>    那就先保护。

⇒ **关键澄清（比本稿 §3.2 的原提议更准确）**：**锚点序列不是"第二种计划条目"，
而是目标条目的"路径限定"**。计划条目**只有一类**（携带逻辑，如 `goto`），
锚点序列是它的**属性**；解析之后就是一条 `route`（有向边序列）。
⇒ 因此不存在"两种载荷并存的复杂度"：**一条目 + 可选路径限定**。

### 3.1 机制与载荷正交 ⇒ 风险③（无法测试）可解

计划条目 = **载荷 + 步进语义**。指针推进、回绕、事件驱动步进（Q13/Q14）、
单一步进者、到达幂等等**机制**与"条目是否有路径限定"**无关**。
⇒ **可以先做"无路径限定"的目标级条目把机制跑通并回归**（今天就有寻路可用），
路径限定是**后加的属性**；路径计划也可先**程序化构造 + 落盘**（测试里直接构造，
不必先有 GUI）。

### 3.2 条目模型（定案方向）

| 层 | 内容 | 谁生产 `route` |
|---|---|---|
| **条目层（逻辑）** | 计划 = 有序条目 + 指针；条目如 `goto(目标)`——**唯一携带逻辑的层** | 无路径限定时：寻路（今天就有） |
| **路径限定层（可选）** | 条目可带**锚点序列**（+ 部分更细控制点）；解析 = 逐段寻路补全 + 控制点钉死 | **有路径限定时：按锚点解析** |
| **执行层（不变）** | `route: list[DirectedEdge]` → 进路预约 → 授权 → 物理 → 位移 | —— |

⇒ **执行层完全不变**（§1.1 已实测），这正是"不切断消费顺序"的结构保证。

### 3.3 锚点与"部分控制点"（Q21-1 = B）

- **锚点**：玩家"点几下"就能表达的**途经点**（道岔/节点/站台端点…，具体要素待 Q1）；
- **控制点**：比锚点更细的钉死（"这一段必须走这几条边/这几个段"，具体粒度待 Q5）；
- 锚点之间的路径**默认由寻路补全**（这是"定义成本从段级降到点级"的关键）；
- 需要完全确定的地方再上控制点 ⇒ **锚点级为主、控制点为辅**，同一份数据结构。

### 3.4 悬空引用：**用户已拍板"拒绝编辑被引用的要素"**（Q21-3 = A）

- 政策：**被计划引用的要素不允许被编辑/删除**（与现有 DELETE 保护同构：
  今天已拒绝删除"被列车 `occupied`/`route` 引用"的边，见 `_train_locked_edges`）。
- 理由（用户）：图变化适配复杂且困难，**现在不适配，就先保护**。
- **待定（Q2）**：引用落在**物理要素（节点/边）**还是**持久化的"段"实体**上——
  这决定保护逻辑挂在谁身上。建议落在物理要素上（段只作派生视图）。
- 复用**几何锚定**：`session.py` 的"有向边不存 `edge_id`、存两端节点坐标"
  （`_directed_to_coords`/`_coords_to_directed`）是现成的跨存档稳定引用机制。

---

## 4. Q21 已拍板（2026-09-10）与**下一轮关键问卷（Q22，5 问）**

### 4.1 Q21 结论（✅ 已定，勿回退）

| 问 | 结论 |
|---|---|
| **Q21-1 粒度** | ✅ **B = 锚点级 + 部分控制点**（锚点为主、控制点为辅） |
| **Q21-2 与目标级的关系** | ✅ **并存，且锚点比目标级"低一层"**——**只有目标条目携带逻辑**，锚点序列**解析之后只是路径**（⇒ 不是第二种条目类型，而是条目的**路径限定属性**） |
| **Q21-3 图变化策略** | ✅ **拒绝编辑/删除被引用的要素**（与现有 DELETE 保护同构；"既然现在不适配，就先保护"） |

### 4.2 下一轮关键问卷（Q22，决定 roadmap 形状的 5 问）

> 这 5 问会改变"新 roadmap 有哪些阶段、哪些是 demo 前置"，所以先问；
> 其余问题（Q17-1/2/3、Q18、连挂条目、步进表细节、编辑时机与生效点、落盘时机、
> 失效显示）留到"按新 roadmap 的二次问卷"，用于**规划封口**。

**Q22-1 锚点用什么要素表达？**（决定 POI 是否成为实现前置）
A（建议）**先用现成图要素**：道岔节点（度数 ≥ 3）、二度节点（= Waypoint）、
站台以"段 + 端点"表达；**不新建实体**，`poi.md` O1~O9 **暂不阻塞实现**，
将来引入 POI 时叠加为"带名字的锚点"。
B **必须先有 POI 实体**（第三桶、随轨道落盘），锚点只能是 POI ⇒ **O1~O9 必须先研讨完**。
C 两者并存（现成要素 + POI 命名锚点，计划里支持两种引用类型）。

**Q22-2 路径的引用与落盘粒度？**（决定"拒绝删除被引用要素"挂在谁身上）
A（建议）**引用落在物理要素**：锚点 = 节点，控制点 = **边序列**；
"段（simple_segment）"只作**派生的显示/编辑单位**，不进存档、不留 id。
B **持久化"段表"**（段有稳定 id、随轨道落盘）：引用指向段 id；代价是段会因
"中途插入节点"而分裂 ⇒ 必须额外定义**段 id 的继承/失效规则**，编辑摩擦更大。
C 折中：段派生，但计划里存"段端节点 + 途经边数"之类**指纹**，解析时校验。

**Q22-3 锚点是硬约束还是倾向？**（决定解析器契约与失败语义）
A（建议）**硬约束**：锚点必须按序经过；解析不出（被删/不可达）⇒ 按 Q11/Q18
处理（跳过该条目 **或** 停车等待），**绝不自动改走别的路**——这才是"路径级确定"的本义。
B **倾向**：尽量走锚点，走不通让寻路绕（更宽容，但确定性打折，
玩家会以为"钉住了其实没钉住"）。
C 每个锚点可标"硬/软"（最灵活，实现与 UI 都更贵）。

**Q22-4 demo 里玩家怎么"创建路径"？**（决定编辑 UI 是否进 roadmap）
A（建议）**最小交互**：选中列车后**依次点击**道岔/节点/路段端点 = 追加锚点
（可回退一步），再按键把它变成"前往 X、途经这些锚点"的计划条目；
**不做面板/列表 GUI**。
B **只做数据层**：demo 不含创建 UI（路径靠程序化/落盘注入，玩家仍用右键单次指令）
——风险：**demo 展示不出"路径级自动驾驶"这个卖点**。
C **完整计划编辑器**（列表 + 拖拽 + 锚点编辑）：最全但工程量最大，会把开源往后拖。

**Q22-5 "控制点"钉到什么粒度？**（决定数据模型）
A（建议）**道岔处选出口**：控制点 = "在道岔 N 走哪条出边"（等价于现实里的
**进路/道岔定反位**）——信息量最小、最像现实、最好做 UI。
B **段级**：显式列出要走的 `simple_segment` 序列（提案原文形态）。
C **边级**：最细最死板（一次轨道编辑就废）。
（可组合：A 作权威表示，B/C 作显示与调试视图。）

---

## 5. 与既有决策的关系（拍板后回填）

- **Q7 单向管道不变**：有路径限定时，"调度计划 → 寻路需求"这一跳被**替换**为
  "计划 → 锚点解析 → 寻路补全"，后四跳（寻路结果 → 预约 → 物理 → 位移）完全照旧。
- **`poi.md` 需回填**：Q21-2 已澄清"锚点是条目的路径限定"（⇒ **不是**第二种条目
  类型）；"路段（Simple_Segment）"的**地位待 Q22-1/Q22-2 定**——若"先用现成图要素"
  （建议），则 POI 暂不阻塞实现，"路段"先以"段 + 端节点"形态充当锚点/控制点。
- **`wagon_centric_data.md` Q15** 需补一句：*条目可带"路径限定"属性（锚点序列）*。
- **roadmap #7（最大 demo 缺口）范围要重写**：由 Q22 答完后产出的**新 roadmap**取代。
- **R2（谁可调用车厢方法/何时）无关**，不受影响。
- `docs/editor.md` 不受影响（编辑器不碰计划；但 Q21-3 的"拒绝编辑被引用要素"
  会让 `Editor` 多一个**外部查询钩子**——注意保持 `Editor` 对列车/计划零耦合的
  既有约定，检查应放在 `GameLoop` 一侧的准入判断里，与 DELETE 保护同构）。

---

## 6. 推进顺序（Q21 已定后的版本；**完整版将由 Q22 答完后产出的新 roadmap 取代**）

| 步骤 | 内容 | 状态 |
|---|---|---|
| A | **段分区 + 校验**（纯模型）：切分规则（度数≠2 **或** 过境转向不许可）、段内可通行校验、段 → edge 序列解析 | ❌ 不被任何待定项阻塞（Q22-1/2/5 都还要它） |
| B | **计划机制**（指针/步进/回绕/事件驱动/遴选）先只支持"无路径限定"的目标级条目 | ❌ 不被阻塞（机制与载荷正交） |
| C | **锚点/控制点解析器**（条目 + 路径限定 → `route`） | ⚠ 取决于 Q22-1/2/3/5 |
| D | **编辑交互**（点锚点成路径） | ⚠ 取决于 Q22-4 |
| E | **"拒绝编辑被引用要素"** 接入 DELETE/BUILD 准入判断 | ⚠ 取决于 Q22-2（保护挂在节点/边上最简单） |

⇒ **A + B 可以马上做**（若用户批准）：它们**不预设 Q22 的任何答案**，
让"计划的骨架"不因架构悬念停摆。
