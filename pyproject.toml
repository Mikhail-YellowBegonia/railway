[project]
name = "railway"
version = "0.1.0"
description = "A 3D-oriented railway sandbox focused on track construction, signalling, flexible consists, and dispatch operations"
readme = "README.md"
authors = [
    { name = "Mikhail" },
]
license = "GPL-3.0-or-later"
license-files = ["LICENSE"]
requires-python = ">=3.11"
dependencies = [
    "pygame-ce>=2.5.7",
    "pygame-gui>=0.6.14",
]
